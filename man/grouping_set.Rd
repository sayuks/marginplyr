% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping-spec.R
\name{grouping_set}
\alias{grouping_set}
\alias{grouping_sets}
\alias{rollup}
\alias{cube}
\alias{grouping_spec}
\title{Build a grouping specification}
\usage{
grouping_set(...)

grouping_sets(...)

rollup(...)

cube(...)

grouping_spec(...)
}
\arguments{
\item{...}{Bare columns, tidy-select expressions, or nested grouping
specifications as appropriate for the constructor.}
}
\value{
A grouping specification for use in \code{.grouping}.
}
\description{
These constructors describe SQL-style grouping operations for the
\code{.grouping} argument of \code{\link[=summarize_with_margins]{summarize_with_margins()}} and related verbs.
}
\details{
\itemize{
\item \code{grouping_set()} creates one grouping set. With no columns it represents
the empty set \verb{()}.
\item \code{grouping_sets()} forms the union of its arguments.
\item \code{rollup()} creates hierarchical prefixes.
\item \code{cube()} creates every subset of its dimensions.
\item \code{grouping_spec()} forms the Cartesian product of its arguments, like
comma-separated SQL \verb{GROUP BY} items.
}

A \code{grouping_set()} nested directly inside \code{rollup()} or \code{cube()} is a
composite dimension. Its columns are added or removed together.
}
\examples{
rollup(region, store)
cube(product, channel)

grouping_sets(
  grouping_set(region, store),
  grouping_set(region, product),
  grouping_set()
)

grouping_spec(
  rollup(region, store),
  cube(product, channel)
)

cube(grouping_set(country, state), year)
}
